{
  "name": "terraform",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "terraform/src",
  "projectType": "library",
  "release": {
    "version": {
      "manifestRootsToUpdate": ["dist/{projectRoot}"],
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/terraform",
        "main": "terraform/src/index.ts",
        "tsConfig": "terraform/tsconfig.lib.json",
        "assets": [
          "terraform/*.md",
          {
            "input": "./terraform/src",
            "glob": "**/!(*.ts)",
            "output": "./src"
          },
          {
            "input": "./terraform/src",
            "glob": "**/*.d.ts",
            "output": "./src"
          },
          {
            "input": "./terraform",
            "glob": "generators.json",
            "output": "."
          },
          {
            "input": "./terraform",
            "glob": "executors.json",
            "output": "."
          }
        ]
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "terraform/jest.config.ts"
      }
    }
  }
}
