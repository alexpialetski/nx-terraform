data "aws_region" "current" {}
data "aws_caller_identity" "current" {}

locals {
  bucket_name = "<%= bucketPrefix %>-${data.aws_caller_identity.current.account_id}-${data.aws_region.current.region}"
}
