// Remote backend template (customize & uncomment for real usage)
/*
terraform {
  backend "s3" {
    bucket = "your-terraform-state-bucket"
    key    = "<%= projectName %>/terraform.tfstate"
    region = "us-east-1"
    encrypt = true
    dynamodb_table = "terraform-locks"
  }
}
*/
